# Joke API Integrator

A simple pipeline to fetch random jokes from an online API and save them to a local SQLite database.

## Features
* Fetches random jokes.
* Saves to SQLite database.
* Prevents duplicate jokes.

## How to run
```bash
